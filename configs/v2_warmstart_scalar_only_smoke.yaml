extends: v2_warmstart_scalar_only.yaml
warmstart:
  max_steps: 2
  max_samples: 8
  epochs: 1
logging:
  run_name: v4_warmstart_scalar_only_smoke
  output_dir: outputs/v4_warmstart_scalar_only_smoke/
  log_every: 1
  save_every: 100
  use_wandb: false
